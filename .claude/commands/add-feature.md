---
description: Add a feature to a running project
argument-hint: <project name> — <feature description>
---
Add a feature. Input: $ARGUMENTS (project name, then the feature). Follow `CLAUDE.md` and `execute.md`: write the feature into the spec, build it (MVP-first), honor the hard build rules and UI rules, test it (`qa`), and update `STATUS.md`. Proceed autonomously; pause only on genuine blockers.
